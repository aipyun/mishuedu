{% if setting('vip.enabled') %}
  <div class="form-group">

    <div class="col-sm-2 control-label">
      <label for="price">{{ '会员规则设置'|trans }}</label>
    </div>
    <div class="col-sm-4 controls">
      <select class="form-control" name="vipLevelId">
        {{ select_options(levels, classroom.vipLevelId, {0:'无'}) }}
      </select>
    </div>

    <div class="col-sm-10 col-sm-offset-2">
      <div class="help-block">{{'设置班级课程后，该会员及更高等级的会员，可免费加入。'|trans}}</div>
    </div>

  </div>
{% endif %}